<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codesses.e_serve">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- To request foreground location access, declare one of these permissions. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- Required only when requesting background location access on Android 10 (API level 29). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eserve">
        <activity
            android:name=".activity.ServicerFeedbackActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".activity.AboutUsActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".serviceProvider.activity.UserRequestActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".activity.ChatRoomActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".activity.RoutingActivity"
            android:exported="true"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".serviceProvider.activity.ServiceEditProfileActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".customer.activity.CustomerEditProfileActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".serviceProvider.activity.ServiceMainActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".customer.activity.CustomerMainActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".auth.SignUpActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".auth.LoginActivity"
            android:theme="@style/AppPrimaryColorTheme" />
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- if v2 use this "com.google.android.maps.v2.API_KEY" -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <service android:name=".service.ForegroundLocationService" />
        <service android:name=".service.LocationService" />
        <service
            android:name=".fcm.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>