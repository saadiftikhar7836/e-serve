<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Eserve" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>


    <!-- Splash screen theme. -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="ToolbarTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="titleTextAppearance">@style/ToolbarTheme.Title</item>
    </style>

    <style name="ToolbarTheme.Title" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/white</item>
    </style>


    <!--    Set app color to the status bar-->
    <style name="AppPrimaryColorTheme" parent="Theme.Eserve">

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/colorGreen</item>
        <item name="android:editTextColor">@color/colorGreen</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="colorAccent">@color/colorGreen</item>
    </style>


    <style name="Grey0ColorTheme" parent="Theme.Eserve">

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/grey0</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="colorAccent">@color/colorGreen</item>
    </style>


    <!--    Full screen bottom sheet fragment  -->
    <style name="FullScreenBottomSheetDialog" parent="Theme.AppCompat.Light.DialogWhenLarge">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@style/Animation.Design.BottomSheetDialog</item>
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">@dimen/_16sdp</item>
        <item name="cornerSizeTopRight">@dimen/_16sdp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <!--    Set height & round corners of modal bottom sheet start-->
    <style name="ServiceProviderInfoBottomSheetTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/ServiceProviderModalStyle</item>

        <!--        For Android 10 to ownWard navigation button issue -->
        <item name="android:windowIsFloating">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/white</item>

    </style>

    <style name="ServiceProviderModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <!--        <item name="behavior_peekHeight">@dimen/_600sdp</item>-->
        <item name="android:background">@drawable/rounded_bottomsheet_design</item>
    </style>

</resources>